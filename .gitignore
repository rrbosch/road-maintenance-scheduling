# Python
__pycache__/
*.pyc
*.pyo

# IDE / editor / assistant
.idea/
.vscode/
.claude/

# Old git history backup (created during the item-10 cleanup; delete when no longer needed)
.git_OLD_backup/

# Scratch / non-core-library files relocated here (throwaway scripts, legacy plotting,
# generated figures, debug artifacts). Not part of the published library.
temp/

# Generated run output & analysis dirs
Experiments/
analysis/output/
plots/

# Downloaded Snellius logs (local landing folders; nothing in-repo writes here)
/hpc log/
/logs/
hpc/logs/

# Active dispatch slot — ephemeral copy of a registry_e<N>.json (submit_array.sh reads this).
# The canonical experiment registries hpc/registry_e{1,2,3,4}.json ARE tracked; this working
# copy is not (copy the one you want to run into it — see hpc/README.txt).
hpc/registry.json
/other/
/results_processing/
/results_processing_rerun/
/results_processing_sensitivity/

# Traffic-results caches (removed in favor of the in-memory FIFO cache)
*.db
*.db-shm
*.db-wal
*.zip

# Scratch plot outputs from Environments/env/__main__ runs
Environments/env/*.png

# Generated ROOT-level artifacts only (root-anchored so Environments/input/** is untouched)
/*.pdf
/*.png
/*.xlsx
/*.docx
/*.ilp
/*.csv
/_ul
/to_do_experiments2.json
/myfirstbash.sh
# Throwaway convenience runner (the B&B solver lives in Src/Algorithms/ and runs via a registry).
/run_sf12_bnb.py
# Stray run/debug logs
*.log

# Excluded from the public repo: not needed to reproduce experiments (kept on disk).
# Internal AI-assistant guidance / empty stub / unused label map.
/CLAUDE.md
/tno_dtp_api.py
/legend.json
# Claude-maintained campaign tracker / working notes (not needed to reproduce experiments).
/EXPERIMENTS.md
# Internal collaboration document (model technical report shared with partners).
/MODEL_TECHNICAL_REPORT.md
# Legacy post-hoc analysis & plotting, superseded by the analysis/ package.
/results_processing2.py
/results_processing_old.py
/visualize_networks.py
Visualization/
# Benchmark/scratch script, not part of the experiment pipeline.
Environments/env/speed_test.py
# TAP2021 reference-network DATA for other cities (experiments use Sioux Falls + Anaheim only);
# the TAP2021 *.py helpers are kept.
Environments/env/TAP2021/tntp_networks/
Environments/env/TAP2021/processed_networks/
